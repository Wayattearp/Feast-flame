import React from 'react';
import { Link } from 'react-router-dom';

function Main() {
    return (
            <main>
                <Link to="/booking">Reserve a table</Link>
            </main>
    );
}

export default Main;
